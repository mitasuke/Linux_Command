参考先：http://ossfan.net/manage/linux-12.html

/etc/httpd/conf/httpd.conf など、設定ファイルは空白行とコメント行が多く、
有効な設定の部分を確認しようとすると、探すのに時間がかかることがちらほらある。

以下を実行すれば、空白行とコメント行（行頭にコメントアウトマーク # がついている行）を除いて、
ファイルの中身を表示してくれるので、有効な部分がわかりやすくなるかも。

# cat ファイル名 | grep -v "^\s*$" | grep -v "^\s*#"

\s：空白（スペース）を表す
^：先頭文字
*：直前文字の0回以上の繰り返し

よって、^\s*# は「先頭からスペースが0回以上続いた後に#がある文字列」を表す。
